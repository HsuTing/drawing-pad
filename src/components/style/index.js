'use strict';

import white from 'cat-components/lib/color/white';

export const rootStyle = {
  width: '100vw',
  height: '100vh',
  overflow: 'hidden'
};

export const pad = {
  width: '100%',
  height: '100%',
  background: white,
  cursor: 'default'
};
